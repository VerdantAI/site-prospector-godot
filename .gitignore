# Godot's import cache and generated artefacts.
.godot/
*.import

# Maps the addon renders on demand. Regenerate them; do not commit them.
demo/generated/

# OS noise
.DS_Store
Thumbs.db

# The required dependency, installed rather than vendored.
addons/automate_godot/

# Per-machine assistant setup. Written by the dock; never committed.
site_prospector.local.cfg
